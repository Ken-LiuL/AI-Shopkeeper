name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -s "https://api.render.com/deploy/srv-d6ei369r0fns73cm03jg?key=3xeIfv2Q7oE"
          echo "Render deploy triggered"

  # Frontend auto-deploys via Vercel GitHub integration
